name: Tests (Newspoint.Tests, manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
      DOTNET_NOLOGO: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore Newspoint.Tests
        run: dotnet restore Newspoint.Tests/Newspoint.Tests.csproj

      - name: Build Newspoint.Tests (Release)
        run: dotnet build Newspoint.Tests/Newspoint.Tests.csproj --configuration Release --no-restore

      - name: Test Newspoint.Tests
        run: dotnet test Newspoint.Tests/Newspoint.Tests.csproj --configuration Release --no-build --logger "trx;LogFileName=test-results.trx"

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            **/TestResults/*.trx
            **/TestResults/**/*.trx
          if-no-files-found: warn
